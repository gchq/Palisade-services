apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: k8s-pathparam-regex
  namespace: {{ .Values.service.namespace }}
spec:
  replacePathRegex:
    regex: ^/kubernetes/(.*)
    replacement: /$1