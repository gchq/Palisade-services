apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.global.persistence.dataStore.name }}
  {{- if eq .Values.hosting "aws" }}
  labels:
    type: amazonEBS
  {{- end }}
spec:
  accessModes:
  - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ .Values.global.persistence.dataStore.name }}
  capacity:
    storage: {{ .Values.global.persistence.dataStore.storage }}
  {{- if eq .Values.hosting "local" }}
  hostPath:
    # Directory location on host
    path: {{ .Values.global.persistence.dataStore.hostPath }}
    type: DirectoryOrCreate
  {{- else if eq .Values.hosting "aws" }}
  csi:
    driver: efs.csi.aws.com
    volumeHandle: {{ .Values.global.persistence.dataStore.volumeHandle }}
    # The path has to be created before hand - see here: https://github.com/kubernetes-sigs/aws-efs-csi-driver/issues/63
    # Only safe mount point is root as it currently stands
    volumeAttributes:
      path: {{ .Values.global.persistence.dataStore.volumePath }}
  {{- end }}
